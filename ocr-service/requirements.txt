# FastAPI and Server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6  # Required for file uploads

# OCR Engine - Tesseract (via pytesseract)
# Tesseract supports Khmer, English, French natively
# Language packs installed via apt: tesseract-ocr-khm, tesseract-ocr-eng, tesseract-ocr-fra
pytesseract>=0.3.10

# Image Processing
pillow>=10.0.0
opencv-python-headless>=4.8.0  # headless version for Docker
numpy>=1.24.0

# Data Validation
pydantic>=2.0.0

# HTTP Client (for AI service calls)
httpx>=0.25.0

# Testing (optional)
pytest>=7.4.0
pytest-asyncio>=0.21.0
